# Two separate sites: RCN and DCT

Read `AGENTS.md` and `SITE-BOUNDARIES.md` before editing or deploying.
RCN is the tracked root website. DCT is only `dct-site/`.
`rcn-site/` is a stale duplicate and must NEVER be deployed.
Never recursively upload this repository. Use the explicit site staging tool.
