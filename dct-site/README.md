# Discount Coach Tours production site

Static HTML/CSS/JS with a PHP lead handler, live at `https://book.discountcoachtours.ca/`.

## Production status

- **(14 Sep 2026, conversion routing repaired; attribution still unverified)** Three real 13 September leads contained both GCLID and GBRAID, but `Build Ads Upload` preferred GBRAID, discarded the available GCLID, and bypassed the Data Manager queue. The live handler now prefers GCLID and uses a privacy ID only when GCLID is absent. All four distinct recent lead GCLIDs matched DCT campaign `24230451785` in Google's `click_view`. Three 13 September leads were replayed with GCLID while retaining their original action `7748270854` and Order IDs, avoiding cross-action duplication. The 12 September canonical order `DCT-20260912-125808-5bd16dd0` was replayed on its already-used recovery action `7762251563`. All four received HTTP 200, nonempty results and no partial failure; these are acceptance receipts, not proof of attribution. Google reporting still showed zero at investigation time. The earlier 12 September submission was the same person and GCLID and is labelled `duplicate_submission` in the lead sheet, with its canonical Order ID noted. Recovered statuses and the mismatched queue action name were reconciled in Sheets. Retraction routing now prefers the recorded action over inferring one from captured IDs, preserving both historic braid uploads and future GCLID uploads. Five regression tests, live published-version readback and a tokenless webhook HTTP 200 check passed. Narrow deployment: `tools/fix_dct_click_routing.py`; recovery: `tools/replay_dct_conversion.py` (validate-only by default, accepted originals cannot switch action). Non-PII recovery receipts with job IDs and `attribution_status: unverified` are retained privately at `/home/ben/infra/dct-conversions/receipts/` on the VPS.
- Approved pages are public and indexable; thank-you and 404 remain `noindex`.
- Separate PPC pages and forms exist for Trafalgar, Globus, Cosmos and Insight Vacations.
- `submit.php` writes every valid lead to `~/dct-private-data/leads.csv` before calling either delivery path.
- The PHP/cPanel handler sends a direct hosting backup email and audits the attempt in `~/dct-private-data/direct-email-deliveries.csv`.
- The n8n handoff is audited separately in `~/dct-private-data/n8n-deliveries.csv`.
- The dedicated n8n webhook is `/webhook/dct-form`, protected by `X-DCT-Token`.
- Production emails route to both `sales@rivercruisenetwork.com` and `btl101@gmail.com` through the hosting backup and n8n. QA submissions (`qa_test=1`) route both copies to Ben at `btl101@gmail.com` only.
- **(10 Sep 2026, superseded 11 Sep - see below)** Google Ads had exactly two reusable account-level negative-keyword lists, both attached to the current DCT campaign: `DCT | Global Country Negatives` (176 broad-match non-approved country/destination terms, including Canada, USA and United States) and `DCT | Global Search Exclusions` (70 job, support, irrelevant-brand and other search exclusions). Kiran's approved destinations are excluded from the country list. All 28 live positive keyword criteria containing Canada or Morocco were removed, and the campaign builder no longer recreates them. The campaign's Canada geographic audience targeting remains in place so Canadian customers can still find the approved European-tour offers. The consolidation script is `tools/consolidate_google_ads_negatives.py`; it must be used for future negative-list changes rather than adding loose campaign criteria.
- **(12 Sep 2026, RECOVERED)** Google Ads OAuth was revoked between the successful 07:50 UTC RCN upload and the failed 11:30 UTC attempt. The consent grant was reissued and the complete OAuth client/token set was deployed to the local tools, n8n (with a force-recreate) and the VPS report/retry secrets. `rivercruise/ops/reissue_google_ads_token.py` now always updates the client ID, client secret and refresh token together, because a new refresh token paired with the old client caused the separate RCN retry worker to remain broken after n8n had recovered. The n8n container and hourly RCN retry worker both passed live token/upload checks. DCT order `DCT-20260912-125808-5bd16dd0` and the two missed RCN orders were accepted by Google after recovery; the retry ledger was reconciled to prevent resends. The RCN workbook's Apps Script property remains a manual fourth copy when its checkbox actions are needed. Meanwhile the first search-terms report (96 terms, 20 clicks, C$45.60) showed the operator groups clean (9 clicks, all relevant) and the Generic HOLD group 100% waste (11 clicks, C$25.59). Three responses are staged in `tools/dct_place_negatives.py`: a fourth list `DCT | Competitor Tour Operators` (24 domestic coach companies and Europe-direct competitors seen in the report: Great Canadian, Front Line, Collette, Go Ahead, Exoticca, CAA...); residence provinces added broad to the country list (Ontario, Quebec, Alberta, Saskatchewan, Manitoba, PEI - reversing the 11 Sep exclusion, because every province query in the report was local product and no brand-plus-province query appeared); `nyc` to the city list; and 11 intent exclusions to the search list (`near me`, `day trip`, `tour bus`, `schedule`, `reviews`, `tour companies`, `long stay`, `free`, `heathrow`, `airport`). Left alone on purpose: `seniors` (the buyer demographic, even though every instance was domestic) and anything naming an approved destination or a future year.
- **(18 Sep 2026)** **AI Max is on** for the campaign (`segments.search_term_match_type = AI_MAX` on most new terms): exact/phrase operator keywords now expand to rival brands, generic and informational queries (`approach tours`, `avalon waterways`, `tours of england` all matched from `trafalgar coach tours` [phrase]). Search terms went 51 to 183 in three days. Consequences: the negative lists are the only fence on query scope, and the Generic HOLD group is redundant (its broad keywords now match the same queries the operator groups' AI Max does). Account: 6 conversions on C$512 / 202 clicks, C$85/lead, ~3% CVR, one a day 14-16 Sep. Generic HOLD converted at C$48/lead (cheapest); Globus is the only operator with 0 on 32 clicks / C$85, landing page worth checking. `collette tours canada` converted via AI Max, i.e. the rival-brand query un-negated on the 15th. Added 25 terms, wrong-product and informational only (rival brands still left to Smart Bidding): `hop on hop off`, `hop on`, `cruise`, `cruises`, `kids`, `children`, `local`, `shorts` (plural of `short`), `within canada`, `east coast`, `alliston`, `omega tours`, `maple holidays`, `time of your life`, `hanover holiday`, `avalon waterways`, and informational phrases `worth it`, `best way`, `best time`, `best places`, `bucket list`, `top 10`, `what is`, `when is`, `how to`. `senior discovery` kept in at Ben's call despite the 15 Sep policy. Lists now 296 / 260 / 32 / 109, all attached, zero conflicts. `trafalgar ca` cost C$6.97 for one click, left alone.
- **(21 Sep 2026)** Added a portfolio Maximize Conversions strategy (`DCT | Portfolio Target CPA 50 | Max CPC 6`) to the live DCT campaign. It preserves the C$50 target CPA and adds a C$6 keyword-bid ceiling after average CPC rose to C$4.55-C$4.67 on 20-21 Sep and individual clicks reached C$10-C$25. The idempotent plan/apply/verify tool is `tools/apply_google_ads_bid_cap.py`. Added `inteletravel` to the wrong-product competitor list; rival Europe tour operators remain eligible under the 15 Sep Smart Bidding policy.
- **(15 Sep 2026)** Third search-terms pull (13-15 Sep: 117 terms, 43 clicks, C$93): zero terms would now be blocked, i.e. the lists are filtering before serving; Generic HOLD down to 1 click; 97% of spend on operator groups. Campaign lifetime 4 conversions on 115 clicks / C$290 (C$72/lead; Trafalgar 2, Insight 1, Cosmos 1; Globus 0 on 26 clicks). Only `viator` added. **Policy change (Ben):** under Smart Bidding, a query naming a rival Europe-direct operator is still a coach-tour buyer and DCT can often sell the same itinerary, so those are a signal source, not waste. Removed `collette`, `go ahead`, `exoticca`, `rabbies`, `approach tours` from the competitor list (31 to 27). Domestic day-trip and bus-ticket companies stay blocked because the product is wrong, not just the brand. Also left alone: `trafalgar reservations`, `cie tours`, `ef tours`, `my cosmos`, `trafalgar ca` and similar navigational/rival-brand terms. Reasoning is recorded at the top of `tools/dct_place_negatives.py`.
- **(12 Sep 2026, evening)** All staged negatives applied: four lists live and attached, zero positive-keyword conflicts. Second search-terms pull (51 terms, 17 clicks, C$60): operator groups now take 16 of 17 clicks, Generic HOLD down to 1. Added from it: 7 more competitors (`flixbus`, `senior discovery`, `dreamtour`, `sunrise tours`, `west world`, `matrix tours`, `stewart travel`) and 8 search exclusions. Three of those exist because **Google negatives do not match plurals or close variants**: the list already had `complaint` and `river cruise` and still let `cosmos travel reviews complaints` and `trafalgar river cruises` through, so `complaints`, `review` and `river cruises` were added alongside. Also `ticket`/`tickets` (bus-ticketing intent) and `website`/`login`/`com` (navigational queries for the operator's own site, e.g. `trafalgar com`, `globusfamily com`). `costsaver` deliberately NOT added: Trafalgar's sister brand under TTC, bookable by any Trafalgar-selling agent. `rcn_report.py --health` gained a third check that reads the n8n `Get Google OAuth Token` node output over the last 24h and alerts on any execution that minted no token, naming the order IDs to backfill; the previous checks tested the report's own credentials and sat under an `expected >= 3` volume gate, which is why the outage read as "All good". Deployed to the VPS.
- **(11 Sep 2026)** North American place negatives added, and the bare broad `Canada` replaced. Negative keywords match the search query, not the searcher's location, so a broad `Canada` on a Canada-targeted campaign was blocking `trafalgar tours canada`, the exact query the headline "Trafalgar Tours Canada" is written for. It was replaced by phrase negatives that still exclude tours *of* Canada (`canada tours`, `tours of canada`, `canada vacation`, `cross canada`...), plus Canadian destination regions and landmarks (Rockies, Banff, Jasper, Niagara, Maritimes, Atlantic provinces, Yukon), all 49 US states, and 14 US national parks, all in `DCT | Global Country Negatives` (176 to 286 terms). A third list, `DCT | Global City Negatives` (257 terms, attached), holds Canadian and US cities, prompted by the search term "short trip toronto". `short` (broad) was added to the search exclusions (70 to 71): there are no short coach tours. The term lists live in `tools/dct_place_negatives.py` and are applied by `tools/apply_google_ads_place_negatives.py` (`--plan` / `--verify` read-only, `--apply` mutates). Two rules the data module enforces: single words are BROAD, multi-word terms are PHRASE (so `new york` cannot touch York in England); and any Canadian or US place that shares a name with an approved destination (London, Windsor, Cambridge, Paris, Naples, Memphis, Alexandria...) is never used as a bare broad term, only qualified by province or state. The provinces people live in (Ontario, Quebec, BC, Alberta, Manitoba, Saskatchewan) are deliberately excluded for the same reason bare `Canada` was. The apply tool refuses to run if any candidate blocks a live positive keyword; at apply time all 369 new terms were checked against 175 serving keywords with zero conflicts. Trade-off to watch in the search terms report: a broad `toronto` also blocks departure-city queries like `coach tours from toronto to europe`.
- **(10 Sep 2026)** `brochure` (broad) was removed from `DCT | Global Search Exclusions`, taking it from 71 terms to 70. A brochure request is buying intent for a tour agency, so excluding it was costing genuine top-of-funnel traffic. Removals go through `tools/consolidate_google_ads_negatives.py --remove-term "<text>"`, which only ever edits the search-exclusion list; the country list holds the destination policy and is not editable that way, so a typo cannot silently unblock a non-approved destination.
- **(10 Sep 2026)** The search campaign runs six ad groups: one per operator (`DCT | Trafalgar`, `DCT | Insight`, `DCT | Globus`, `DCT | Cosmos`), plus `DCT | Brand` and `DCT | Generic Coach Tours | HOLD`. The paired `Brand & Tours` / `Destinations` groups were merged one-per-operator to stop an already-thin conversion signal being split eight ways on a C$65/day budget. Each merged group holds both keyword sets and both RSAs. The four `| Destinations` groups were PAUSED, not removed, so the merge is reversible. The tool is `tools/consolidate_google_ads_ad_groups.py` (`--plan` / `--verify` are read-only, `--apply` mutates).
- **(10 Sep 2026)** While copying the destination RSAs into the merged groups, ad copy naming destinations DCT does not sell was replaced with approved ones: Morocco headlines and description mentions on Trafalgar and Insight, and 'Canada Tours' headlines and description mentions on Globus and Cosmos. Those assets predated the destination cleanup and were still serving. The originals survive in the paused groups. Note these came from later manual edits, not from `build_google_ads_campaign.py`, whose own `dest_heads` were always clean.
- **(10 Sep 2026)** `tools/build_google_ads_campaign.py` was brought back in sync with the live account and is a usable rebuild path again. It now emits the six-group structure with two RSAs per operator group, uses the live `| HOLD` generic group name, and attaches both negative shared sets via `ensure_shared_sets()`. That attachment matters: after the negatives moved out of the builder, a rebuild would otherwise have produced a campaign with no negative coverage at all. `ensure_ads()` keys on `(ad group, path2)` and only ever creates missing ads, so hand-edited live copy is never overwritten. `--verify` now reports `serving_*` counts alongside raw totals (raw counts include PAUSED groups and REMOVED criteria, so quote the `serving_` figures) plus `negative_coverage_ok`.
- UTMs, Google ValueTrack fields, separate GCLID/GBRAID/WBRAID values, landing page, referrer, operator, source page, device and time on page are captured. A first-party 90-day attribution cookie preserves the visit across pages, while a newer campaign visit replaces an older stored click bundle.
- Email and phone are normalized and SHA-256 hashed server-side for a future Enhanced Conversions for Leads rollout. Those identifiers are not sent to Google until DCT approves the disclosure and accepts Google's Customer Data Terms.
- The retained Google Ads client is `363-922-5242` under Copperchunk MCC `381-427-8874`. The later accidental duplicate `536-272-2800` was cancelled on 5 September 2026.
- GCLID leads use `DCT - Submit Lead Form (Data Manager - GCLID)` (`7762251563`, ONE_PER_CLICK). The intake writes the durable [DCT Google Ads Conversion Queue](https://docs.google.com/spreadsheets/d/12NZo1KjNDeuMUk-0f0AirO5lNZD0AssWf41kRTUXGh8/edit) first, then attempts the same action immediately through the API. Data Manager rereads active rows daily, so an API outage is retried without duplicating a conversion: both paths use the same action and Order ID. Privacy-preserving GBRAID/WBRAID leads continue to use `DCT - Submit Lead Form (Offline - Braid)` (`7748270854`, MANY_PER_CLICK) through the API.
- The live `DCT Form Handler` workflow (`PmntlqBanV9ZMy3O`) has 22 nodes: secured intake, hashed email-action token, lead email, private lead-sheet append, Data Manager queue-first persistence, immediate Google OAuth/upload, Ads outcome update, partial-failure-first parsing and route-specific Ben-only alerts. Successful immediate GCLID uploads change the queue row to `uploaded_api`; failures remain `active` for Data Manager to retry, so the same Order ID cannot be imported twice through both paths.
- The live `DCT Lead Email Actions` workflow (`Gd8fvkAJjC4rJuJy`) is isolated from intake. It provides two-step good/bad buttons in production lead emails and records confirmed actions in the `Lead Actions` tab. Marking a GCLID lead bad immediately changes its Data Manager row from `active` to `retracted`, then queues an Order-ID retraction against action `7762251563`. After the 24-hour-plus-30-minute safety buffer, the worker retries transient, authentication and `CONVERSION_NOT_FOUND` failures with backoff until Google accepts the retraction or the 54-day safety limit is reached. Ben is alerted on the first retry, at spaced retry milestones and on terminal failures. Opening or previewing an email never mutates data.
- The private DCT lead spreadsheet is [Discount Coach Tours Leads & Retractions](https://docs.google.com/spreadsheets/d/1dY5JczBg7qkxMov1HmbPRlOpwPrrKwSndh5NiYXfVTY/edit). `Leads` contains the lead and Ads outcome projection; `Lead Actions` is the append-only confirmation/retraction queue; `Instructions` explains the workflow. Raw email action tokens are never stored in the sheet or CSV, only their SHA-256 digest.
- DCT-specific GA4/GTM remains optional and needs a DCT measurement ID/container if browser analytics is required. Offline Google Ads conversion tracking does not depend on it.

## Google Ads API status

The integration targets Google Ads API `v25`, the current major REST version as of 5 September 2026. Google released the non-breaking `v25.1` update on 19 August, but REST endpoints remain `/v25`. Version 25 is scheduled to sunset in August 2027. Upload requests always use `partialFailure: true`, inspect `partialFailureError` before `results`, use Google-compatible space-separated timestamps, identify the conversion environment as `WEB`, and support `validateOnly` QA without recording a conversion.

The current click-only path sends exactly one identifier: GCLID when available, otherwise GBRAID or WBRAID. GCLID immediate uploads and Data Manager retries share action `7762251563` and the same Order ID for idempotency. Hashed email/phone remain gated on DCT authorization. Capturing multiple IDs never causes the available GCLID to be discarded; historical recoveries preserve their original action and Order ID rather than moving accepted events between actions.

Google's post-15-June-2026 offline-upload eligibility change does not block Copperchunk's developer token because the token already has successful RCN upload history. On 5 September 2026, a live `validateOnly` request returned HTTP 200 from the conversion service and the expected `UNPARSEABLE_GBRAID` partial failure for a deliberately fake ID. This confirms OAuth, developer-token access, account routing, v25 request shape and partial-failure handling without recording a conversion. Final n8n execution `84421` also confirmed braid routing, no user identifiers, successful lead email, rejected-upload detection and the isolated failure alert. Execution `84409` confirmed ordinary QA leads bypass OAuth and Ads entirely while email still succeeds. Both PHP-to-n8n deliveries returned HTTP 200. A real conversion cannot be attribution-tested until there is a genuine click from a live DCT campaign; newly created actions also have Google's normal six-hour activation window.

## Deployment

The cPanel document root is `/home/unitcostdominanc/book.discountcoachtours.ca/`. Secrets and lead files must remain outside it in `/home/unitcostdominanc/dct-private-data/`.

Before upload, set directories to `755` and files to `644`. Never upload tools, documentation, lead CSVs or secrets into the public document root. After deployment, test every HTML, CSS, JS and image URL for HTTP 200.

Rebuild operator pages with `python3 tools/build_operator_pages.py`.

Configure or audit the Ads account with `python3 tools/google_ads_offline.py --validate-config`; `--apply` is idempotent after the target customer is explicitly confirmed. Deploy the DCT n8n workflows from the VPS (this updates both the main handler and the isolated email-action/retraction workflow):

```bash
python3 deploy_dct_email_actions.py --phase all
```

The compatibility entry point `python3 deploy_n8n_workflow.py` still patches the main handler only. Do not use it to recreate the workflow from scratch: the email-action deployment script is the authoritative source for the combined setup.

For a non-sensitive node-level QA summary, run `python3 inspect_n8n_execution.py EXECUTION_ID` on the n8n VPS. It reports routing and error classifications without printing PII, click IDs, OAuth tokens or other secrets.
